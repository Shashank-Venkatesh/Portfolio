Fonts (Inter & JetBrains Mono) are loaded via Google Fonts in `index.html`.
Drop self-hosted font files here and update `index.html` / `index.css`
if you'd rather bundle them locally (e.g. for offline builds or stricter CSP).
